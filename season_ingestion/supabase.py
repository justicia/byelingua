from __future__ import annotations


def apply_events(events: list[dict]) -> int:
    """Production canonical writer is intentionally out of scope for this PR."""
    raise RuntimeError("production writer not implemented")
